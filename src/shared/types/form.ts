export type formFields = {
  name: string;
  phone: number | string | null;
  email: string;
  city: string | number | null;
};

export type formatObject = {
  name: string;
  phone: string;
  email: string;
  city_id: number;
};
